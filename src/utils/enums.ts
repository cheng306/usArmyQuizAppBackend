/* eslint no-shadow: 0 */
/**
 * Enum of UnitType
 */
export enum UnitType {
  DIVISION = 'division',
  BRIGADE = 'brigade',
  BATTALION = 'battalion',
  COMPANY = 'company'
}

/**
 * Enum of questions polarity
 */
export enum Polarity {
  POSITIVE,
  NEGATIVE,
}
